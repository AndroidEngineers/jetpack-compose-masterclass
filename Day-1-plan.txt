📘 Day 1 — Compose Foundations (Deep Dive Topics List)
1. setContent Internals

setContent() entry point

ComposeView creation & reuse

Activity → Window → DecorView → Content → ComposeView flow

ViewTree owners (Lifecycle, ViewModelStore, SavedState)

2. Gap Buffer

What is a gap buffer

Why Compose uses it

Benefits for dynamic UI structure

3. Slot Table

What Slot Table stores

How slots map to Composables

Initial composition vs recomposition

Dirty slots & efficient updates

4. remember

Purpose of remember

remember(key) mechanism

Slot-based caching

changed() logic

cache() mechanism

5. mutableStateOf

Mutable state objects

State read/write tracking

Invalidations & state-driven UI

6. Recomposition

What triggers recomposition

How Compose skips unnecessary recompositions

Subtree recomposition behavior

Recomposition counters demo

7. Live Coding Demo

Simple counter app

Debug recomposition

Observe slot memory behavior (mentally)
